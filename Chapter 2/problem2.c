#include <stdio.h>

int main()
{
    int x = 2, y = 3, z = 3, k = 1;
    // float e = 3 * x / y - z + k;
    // printf("The value of e is %f", e);
    
    printf("The evaluation of 3*x/y-z+k is %f", 3 * x / y - z + k);

    /* Calculate and print the arithmetic average of three float values
     float x = 2.0, y = 3.0, z = 3.0;
     float a = (x+y+z)/3;
     printf("The value of a is %f", a);
    */
    return 0;
}